<ol class="steps">
  <li>Download the latest <code>YTXP-vX.Y.Z.zip</code> from the <a href="https://github.com/glhrmbg/yt-experience/releases" target="_blank" rel="noreferrer">Releases page</a> and unzip it.</li>
  <li>Open <code>brave://extensions</code> (or <code>chrome://extensions</code>) and enable <strong>Developer mode</strong>.</li>
  <li>Click <strong>Load unpacked</strong> and select the unzipped folder.</li>
  <li>Open the extension's popup on any tab to toggle features on or off.</li>
</ol>

<style>
  .steps {
    padding-left: 1.25rem;
    color: var(--muted);
    display: grid;
    gap: 0.5rem;
  }

  .steps code {
    background: color-mix(in srgb, var(--fg) 8%, transparent);
    padding: 0.1rem 0.4rem;
    border-radius: 4px;
    font-size: 0.9em;
  }

  .steps a {
    color: var(--accent);
  }
</style>
